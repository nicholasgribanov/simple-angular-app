import { UserUniqueValidatorDirective } from './user-unique-validator.directive';

describe('UserUniqueValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UserUniqueValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
